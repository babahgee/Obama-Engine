import { RenderObject } from "../renderer/canvas.js";

export class pt_controllers_raycast {
    /**
     * Creates an raycast instance. This will handle events when current instance gets in action.
     * @param {RenderObject} renderobject
     * @param {Function} listener
     */
    constructor(renderobject, listener) {

    }
}